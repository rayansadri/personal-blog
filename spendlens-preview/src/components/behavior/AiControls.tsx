import {useState} from 'react';
export function AiControls({interpretation}:any){const [open,setOpen]=useState(false);return <section className="rounded-xl bg-surface p-5"><p className="text-[15px] font-medium">Numbers first. Explanations second.</p><p className="mt-2 text-[13px] text-ink-secondary">This demo computes spending patterns in your browser. The full app can use AI to explain these same signals.</p><button className="mt-3 text-[13px] text-accent" onClick={()=>setOpen(!open)}>{open?'Hide':'Inspect'} the evidence</button>{open&&<pre className="mt-3 max-h-72 overflow-auto text-[12px]">{JSON.stringify(interpretation.input,null,2)}</pre>}</section>}
